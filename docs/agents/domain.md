# Domain Docs

This is a single-context repository.

Before exploring a feature area, read the root `CONTEXT.md` and the relevant ADRs in `docs/adr/`.

Use the terms defined in `CONTEXT.md` in specs, tickets, test names, and code. Surface any conflict with an ADR instead of silently overriding it.
